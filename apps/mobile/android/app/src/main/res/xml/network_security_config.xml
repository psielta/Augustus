<?xml version="1.0" encoding="utf-8"?>
<!--
  Augustus mobile network security.

  Por padrao em Android 9+ o app NAO permite cleartext (HTTP) — o backend
  Augustus em producao deve usar HTTPS (via `--dart-define=API_BASE_URL=https://...`).

  As excecoes abaixo permitem cleartext APENAS para hosts de
  desenvolvimento (emulator/dispositivo dev), nunca para qualquer host
  publico:
    - 10.0.2.2: loopback do host visto pelo Android emulator
    - localhost / 127.0.0.1: usado em alguns setups de tunel/proxy
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
